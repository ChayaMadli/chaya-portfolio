// Shared Vitest setup for the React app tests.
// The current project keeps assertions explicit and deterministic,
// avoiding custom matcher registration issues while still testing
// accessible UI behavior via Testing Library queries.
